// Initialize the service
class Service {
    // This method handles the request
    void handleRequest() {
        // Create a new connection
        var conn = new Connection();
    }

    // Step 1: Validate input
    void process() {}

    // --- Helpers ---
    void helper() {}

    // Thread-safety: synchronized on `this` to prevent concurrent modification
    synchronized void safeMethod() {}
}
