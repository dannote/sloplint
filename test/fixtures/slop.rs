// Initialize the vector
let mut v = Vec::new();

// Add items to the vector
v.push(1);

// This function handles the computation
fn compute() -> i32 {
    // Calculate the sum
    42
}

// Step 1: Parse the input
fn process() {}

// --- Helpers ---
fn helper() {}

// SAFETY: pointer is guaranteed valid by the borrow checker invariant above
unsafe fn raw_access(ptr: *const u8) -> u8 {
    *ptr
}
